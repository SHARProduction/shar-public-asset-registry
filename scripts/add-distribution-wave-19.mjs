import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),targetPath=path.join(root,'data/distribution.json'),wavePath=path.resolve(root,'../distribution-wave-19/distribution/wave.json'),target=JSON.parse(fs.readFileSync(targetPath,'utf8')),wave=JSON.parse(fs.readFileSync(wavePath,'utf8')),ensureAbsent=(rows,key,value)=>{if(rows.some(row=>row[key]===value))throw new Error(`${value} already exists`)};for(const work of wave.works)ensureAbsent(target.works,'work_id',work.work_id);for(const representation of wave.representations)ensureAbsent(target.representations,'representation_id',representation.representation_id);for(const placement of wave.placements)ensureAbsent(target.placements,'placement_id',placement.placement_id);ensureAbsent(target.releases,'release_id',wave.release.release_id);target.works.push(...wave.works.map(work=>({...work,title:work.title.en,title_ru:work.title.ru})));target.representations.push(...wave.representations);target.releases.push({...wave.release,work_ids:wave.works.map(work=>work.work_id)});target.placements.push(...wave.placements.map(placement=>({...placement,release_id:wave.release.release_id,counted_in_baseline:true})));for(const placement of wave.placements){const suffix=placement.placement_id.replace('shar.placement.','').replaceAll('.','-');target.link_observations.push({link_observation_id:`shar.link.${suffix}.website`,source_url:placement.public_url,target_url:'https://sharprod.com/',observed_at:placement.verified_at,ownership_group:placement.ownership_group,independent:false})}Object.assign(target.counts,{verified_counted_placements:1302,gap_to_minimum:198,github_public_repositories:152,hugging_face_datasets:20,localized_production_troubleshooting_pages:100,production_troubleshooting_repository_placements:1,production_troubleshooting_dataset_placements:1,indexnow_accepted_url_submissions:308});target.updated_at=[target.updated_at,...wave.placements.map(row=>row.verified_at)].filter(Boolean).sort().at(-1);target.counting_note='Verified rollup: 580 phase-1 + 12 integration recipes + 12 conformance suites + 12 compatibility research + 22 data contracts + 22 reference corpora + 22 workflow recipes + 22 media conformance + 22 validation benchmarks + 4 validation error explorer + 52 production evidence records + 52 production verification workflow placements + 52 production conformance catalog placements + 52 production readiness scorecard placements + 4 indexing observatory placements + 52 production option matrix placements + 52 production risk simulator placements + 52 production capacity planner placements + 102 production glossary placements + 102 production troubleshooting placements. Registry records, navigation pages and releases are not counted again.';fs.writeFileSync(targetPath,JSON.stringify(target,null,2)+'\n');console.log(`merged ${wave.release.release_id}: ${target.counts.verified_counted_placements} verified placements`);
